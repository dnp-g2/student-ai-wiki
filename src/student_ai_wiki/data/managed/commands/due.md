---
description: Show what is due: overdue items, the next 14 days, work to start now, exam readiness and grades
argument-hint: [optional course code, e.g. COMP9417]
---

Use the wiki-tracker skill to run `student-wiki tracker brief` (add `--course` when a course code is given) and present it: $ARGUMENTS

Relay the briefing lines as printed, then suggest the single most useful next action. Offer the `review` and `exam-prep` runs it names when an exam has weak concepts.
